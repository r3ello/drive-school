spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.bellgado.calendar: DEBUG
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
